import styled from 'styled-components'

const Container = styled.View`
  padding-top: 20px;
`

const ImageContainer = styled.View`
  background-color: #d9d9d9;
  border-radius: 15px;
  overflow: hidden;
`

const LogoContainer = styled.View`
  position: absolute;
  top: 0;
  align-self: center;
`

export { Container, ImageContainer, LogoContainer }
