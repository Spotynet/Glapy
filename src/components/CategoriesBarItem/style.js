import styled from 'styled-components'

const Container = styled.View``

const Title = styled.Text`
  font-size: 12px;
  font-weight: 400;
`

const Indicator = styled.View`
  width: 100%;
  height: 2px;
  background-color: red;
  margin-top: 2px;
`

export { Container, Title, Indicator }
